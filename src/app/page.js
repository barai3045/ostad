import React from 'react';
import ProductList from "@/component/ProductList";

const Page = () => {
    return (
        <div>
            <ProductList/>
        </div>
    );
};

export default Page;